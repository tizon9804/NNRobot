#!/usr/bin/env node
var debug = require('debug')('ESATD3');
var app = require('../app');
//process.env.PORT ||


var server = app.listen(app.port(), function() {
    debug('Express server listening on port ' + server.address().port);
});
